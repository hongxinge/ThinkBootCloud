server:
  port: 9000

spring:
  application:
    name: think-boot-gateway

  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:}
        group: DEFAULT_GROUP

    gateway:
      discovery:
        locator:
          enabled: false
          lower-case-service-id: true

      routes:
        - id: think-boot-auth
          uri: lb://think-boot-auth
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1

        - id: think-boot-system
          uri: lb://think-boot-system
          predicates:
            - Path=/api/system/**
          filters:
            - StripPrefix=1

        - id: think-boot-user
          uri: lb://think-boot-user
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=1

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_UNIQUE

  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: 0

thinkboot:
  gateway:
    skip-auth-paths:
      - /api/auth/login
      - /api/auth/register
      - /api/auth/refresh
      - /api/public/**
      - /actuator/**
      - /v3/api-docs/**
      - /swagger-ui/**
      - /swagger-resources/**

    cors-allowed-origins:
      - http://localhost:3000
      - http://localhost:8080
      - http://127.0.0.1:3000
      - http://127.0.0.1:8080

    rate-limit-enable: false

    # JWT密钥（Base64编码，至少32位）
    # 开发环境使用默认值，生产环境请通过环境变量覆盖
    jwt-secret: ${JWT_SECRET:dGhpbmstYm9vdC1jbG91ZC1qd3Qtc2VjcmV0LWtleS1tdXN0LWJlLWF0LWxlYXN0LTI1Ni1iaXRz}

logging:
  level:
    com.thinkboot.gateway: debug
    org.springframework.cloud.gateway: info
